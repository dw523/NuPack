module.exports = {
  /**
   * Calculate the markup for a given project given the following rules:
   * @param project
   * @returns {string}
   */
  calculateMarkup: function (project) {
    var result = '';

    return result;
  }
};
